# .gitignore folder will ignore these folders from sending to Github
virtual_env
# virtual environment with python librarires installed that we need for the project. 
.env 
# environment for sensitive data (api keys, environmental variables). 

